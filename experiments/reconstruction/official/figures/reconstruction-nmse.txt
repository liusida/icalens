Held-out token-level top-k reconstruction, averaged equally across selected layers and evaluation datasets. The plotted metric is Normalized MSE. Stars mark each method's untruncated reconstruction: the complete basis for ICA, PCA, and Random, and native SAE sparsity. For GPT-2, the dashed SAE curve is a training-context control evaluated only at token positions 0–63; solid curves use the full 1,024-position context.
